import { styled } from '../../styles/stitches.config';

export const ImageWrapper = styled('div', {
  width: '240px',
  height: '240px',
  position: 'relative',

  '@dsk2': {
    width: '320px',
    height: '320px',
  },
});
